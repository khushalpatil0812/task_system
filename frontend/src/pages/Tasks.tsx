import {useEffect,useState} from 'react';import {api} from '../api';import {useAuth} from '../auth';import TaskModal from '../components/TaskModal';import type {Project,Task,User} from '../types';import {Plus,Search,Trash2} from 'lucide-react';import toast from 'react-hot-toast';
export default function Tasks(){const {user}=useAuth();const [tasks,setTasks]=useState<Task[]>([]),[projects,setProjects]=useState<Project[]>([]),[users,setUsers]=useState<User[]>([]),[selected,setSelected]=useState<Task|undefined>(),[open,setOpen]=useState(false),[q,setQ]=useState(''),[status,setStatus]=useState(''); const load=()=>api.get('/tasks',{params:{search:q||undefined,status_filter:status||undefined}}).then(r=>setTasks(r.data));useEffect(()=>{load()},[q,status]);useEffect(()=>{api.get('/projects').then(r=>setProjects(r.data));if(user?.role==='admin')api.get('/users').then(r=>setUsers(r.data));else setUsers(user?[user]:[])},[user]);const del=async(id:number)=>{if(!confirm('Delete this task?'))return;await api.delete(`/tasks/${id}`);toast.success('Task deleted');load()};return <><div className="page-title"><div><h1>Tasks</h1><p>Track, prioritize, and complete work.</p></div>{user?.role==='admin'&&<button className="primary" onClick={()=>{setSelected(undefined);setOpen(true)}}><Plus size={18}/> Create task</button>}</div><div className="filters"><label className="search"><Search size={18}/><input placeholder="Search tasks…" value={q} onChange={e=>setQ(e.target.value)}/></label><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">All statuses</option><option>Todo</option><option>In Progress</option><option>Completed</option></select></div><section className="panel"><div className="table-wrap"><table><thead><tr><th>Task</th><th>Priority</th><th>Project</th><th>Assignee</th><th>Due date</th><th>Status</th><th></th></tr></thead><tbody>{tasks.map(t=><tr key={t.id}><td><b>{t.title}</b><small>{t.description}</small></td><td><span className={`priority ${t.priority.toLowerCase()}`}>{t.priority}</span></td><td>{t.project?.name}</td><td>{t.assignee?.name||'—'}</td><td>{t.due_date||'—'}</td><td><select className="status-select" value={t.status} onChange={async e=>{try{await api.put(`/tasks/${t.id}`,{status:e.target.value});load();toast.success('Status updated')}catch{toast.error('Cannot update this task')}}}><option>Todo</option><option>In Progress</option><option>Completed</option></select></td><td>{user?.role==='admin'&&<span className="actions"><button onClick={()=>{setSelected(t);setOpen(true)}}>Edit</button><button className="text-danger" onClick={()=>del(t.id)}><Trash2 size={16}/></button></span>}</td></tr>)}</tbody></table></div></section>{open&&<TaskModal task={selected} projects={projects} users={users} onClose={()=>setOpen(false)} onSaved={load}/>}</>}
